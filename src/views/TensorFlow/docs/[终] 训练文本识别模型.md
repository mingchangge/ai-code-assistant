## 步骤 1: 连接到Google Drive并设置环境

这是第一步，也是最重要的一步。

1.  在本地，将您用上一份脚本生成的整个 `ocr_dataset_hybrid` 文件夹**压缩成一个ZIP文件**，例如 `ocr_dataset_hybrid.zip`。这对于上传至关重要，直接上传几万个小文件会非常缓慢且容易失败。
2.  打开Google Drive，创建一个名为 `train_ocr_model` 的新文件夹。
3.  将 `ocr_dataset_hybrid.zip` **上传**到这个 `train_ocr_model` 文件夹中。
4.  现在，打开一个新的Colab Notebook，连接到GPU运行时，然后运行以下代码单元格来挂载您的Drive。

- 挂载Google Drive

  ```python
  from google.colab import drive
  # --- 1. 挂载 Drive 并设置环境 ---
  drive.mount('/content/drive')
  ```

- 导入必要的库

  ```python
  import os
  import torch
  import torch.nn as nn
  import torch.optim as optim
  import torch.nn.functional as F
  from torch.utils.data import Dataset, DataLoader, random_split
  from torchvision import transforms
  from PIL import Image
  import numpy as np
  from tqdm import tqdm
  ```

- 将数据集从Drive高速复制到Colab本地磁盘

```python
# 定义源路径 (在你的慢速Drive里) 和目标路径 (在Colab的超高速本地磁盘/content/下)
DRIVE_ZIP_PATH = '/content/drive/MyDrive/train_ocr_model/ocr_dataset_hybrid.zip'
LOCAL_ZIP_PATH = '/content/ocr_dataset_hybrid.zip'
LOCAL_DATASET_DIR = '/content/ocr_dataset_hybrid' # 这是最终数据存放的地方

print("🚀 开始将数据集从Drive复制到Colab本地...")
# 使用rsync进行快速、可续传的复制。这比普通的cp命令更快。
# 这一步是整个优化的关键，它将一个大文件从慢速网络盘搬到本地。
!rsync -a --info=progress2 "{DRIVE_ZIP_PATH}" "{LOCAL_ZIP_PATH}"
print("✅ 数据集压缩包已高速复制到Colab本地。")
```

## 步骤 2: 解压数据集

- 解压数据集压缩包

```python
if os.path.exists(LOCAL_ZIP_PATH) and not os.path.exists(LOCAL_DATASET_DIR):
    print("🚀 开始在本地高速解压数据集...")
    # 将本地的zip文件解压到本地的目录
    !unzip -q "{LOCAL_ZIP_PATH}" -d "{LOCAL_DATASET_DIR}"
    print("✅ 数据集已在本地解压完成。")
else:
    print("✅ 数据集已在本地存在，无需重复解压。")
```

- 检查数据集目录结构

  `!unzip` 命令的行为可能和预期的不一样。当解压 `ocr_dataset_hybrid.zip` 时，它可能在 `/content/` 目录下创建了一个额外的、与压缩包同名的父文件夹。

  ```python
  # 检查解压后的目录结构
  !ls -R "{LOCAL_DATASET_DIR}"
  ```

- 将数据集目录调整为直接包含图像和标签文件

  ```bash
  !mv /content/ocr_dataset_hybrid/ocr_dataset_hybrid/* /content/ocr_dataset_hybrid/
  ```

  ```bash
  !rm -rf /content/ocr_dataset_hybrid/ocr_dataset_hybrid
  ```

## 步骤 3: 配置与字符集

```python
# --- 3. 配置与字符集 ---
DEVICE = torch.device("cuda" if torch.cuda.is_available() else "cpu")
print(f"✅ 将使用设备: {DEVICE}")
CHARSET = " 0123456789.%BMI对比上次测量体重公斤脂肪率水分骨骼肌蛋白质肉内脏指数皮下去身年龄型基础代谢活动建议控制偏胖高低标准肥大卡隐形微稍瘦强壮过力发达%()-:（）：-日期健康弱正常"

characters = sorted(list(set(list(CHARSET))))
# PyTorch的CTCLoss要求第一个token是blank
characters.insert(0, "[BLANK]")
with open('charset.txt', 'w', encoding='utf-8') as f:
    f.write("".join(characters[1:])) # 保存时不包含blank

char_to_int = {char: i for i, char in enumerate(characters)}
int_to_char = {i: char for i, char in enumerate(characters)}
VOCAB_SIZE = len(characters)
```

## 步骤 4: 定义PyTorch Dataset

```python
# 定义PyTorch Dataset
class OCRDataset(Dataset):
    def __init__(self, data_dir, transform=None):
        self.data_dir = data_dir
        self.transform = transform
        self.image_paths = []
        self.labels = []
        labels_path = os.path.join(data_dir, "labels.txt")
        with open(labels_path, "r", encoding="utf-8") as f:
            for line in f:
                path, label = line.strip().split("\t")
                self.image_paths.append(os.path.join(data_dir, path))
                self.labels.append(label)

    def __len__(self):
        return len(self.labels)

    def __getitem__(self, idx):
        img_path = self.image_paths[idx]
        label = self.labels[idx]
        image = Image.open(img_path).convert("L") # 直接转为灰度
        if self.transform:
            image = self.transform(image)

        label_tensor = torch.LongTensor([char_to_int[c] for c in label])
        return image, label_tensor
```

## 步骤 5: 定义数据变换与加载器

```python
IMAGE_HEIGHT = 64
IMAGE_WIDTH = 256
BATCH_SIZE = 128

transform = transforms.Compose([
    # --- 新增的在线数据增强 ---
    # 1. 轻微的仿射变换 (旋转、平移、缩放)
    transforms.RandomAffine(
        degrees=1.5,           # 旋转角度限制在 ±1.5 度
        translate=(0.05, 0.05), # 水平和垂直平移最多 5%
        scale=(0.95, 1.05)     # 缩放范围在 95% 到 105%
    ),

    # 2. 颜色抖动 (亮度、对比度)
    transforms.ColorJitter(
        brightness=0.2,
        contrast=0.2
    ),

    # --- 原有的必要步骤 ---
    transforms.Resize((IMAGE_HEIGHT, IMAGE_WIDTH)),
    transforms.ToTensor(), # 将图片转为 [0, 1] 的Tensor
])

full_dataset = OCRDataset(LOCAL_DATASET_DIR, transform=transform)
train_size = int(0.8 * len(full_dataset))
val_size = len(full_dataset) - train_size
train_dataset, val_dataset = random_split(full_dataset, [train_size, val_size])

# Collate_fn 用于处理变长标签
def collate_fn(batch):
    images, labels = zip(*batch)
    images = torch.stack(images, 0)
    label_lengths = torch.LongTensor([len(l) for l in labels])
    labels = nn.utils.rnn.pad_sequence(labels, batch_first=True, padding_value=0)
    return images, labels, label_lengths

train_loader = DataLoader(train_dataset, batch_size=BATCH_SIZE, shuffle=True, collate_fn=collate_fn)
val_loader = DataLoader(val_dataset, batch_size=BATCH_SIZE, shuffle=False, collate_fn=collate_fn)

print("✅ PyTorch数据加载器准备就绪。")
```

## 步骤 6: 划分训练集与验证集

```python
# 1. 创建完整数据集
full_dataset = OCRDataset(LOCAL_DATASET_DIR, transform=transform)

# 2. 【核心】使用 random_split 进行划分
train_size = int(0.8 * len(full_dataset))
val_size = len(full_dataset) - train_size
train_dataset, val_dataset = random_split(full_dataset, [train_size, val_size])

# 3. 为训练集和验证集分别创建 DataLoader
train_loader = DataLoader(train_dataset, batch_size=BATCH_SIZE, shuffle=True, collate_fn=collate_fn)
val_loader = DataLoader(val_dataset, batch_size=BATCH_SIZE, shuffle=False, collate_fn=collate_fn) # 验证集通常不需要打乱

print(f"✅ 数据已划分为: {len(train_dataset)} 训练样本, {len(val_dataset)} 验证样本。")
```

## 步骤 7: 构建CRNN模型

```python
EPOCHS = 200;
PATIENCE = 15;
best_val_loss = float('inf');
patience_counter = 0
MODEL_SAVE_PATH = "best_crnn_websafe_model.pth"

# --- 4. 【核心替换】定义全新的、Web友好的模型架构 ---
class ResNetBlock(nn.Module):
    def __init__(self, in_channels, out_channels, stride=1):
        super(ResNetBlock, self).__init__()
        self.conv1 = nn.Conv2d(in_channels, out_channels, kernel_size=3, stride=stride, padding=1, bias=False)
        self.bn1 = nn.BatchNorm2d(out_channels)
        self.conv2 = nn.Conv2d(out_channels, out_channels, kernel_size=3, stride=1, padding=1, bias=False)
        self.bn2 = nn.BatchNorm2d(out_channels)

        self.shortcut = nn.Sequential()
        if stride != 1 or in_channels != out_channels:
            self.shortcut = nn.Sequential(
                nn.Conv2d(in_channels, out_channels, kernel_size=1, stride=stride, bias=False),
                nn.BatchNorm2d(out_channels)
            )

    def forward(self, x):
        identity = self.shortcut(x)
        out = F.relu(self.bn1(self.conv1(x)))
        out = self.bn2(self.conv2(out))
        out += identity  # 简单的加法，ONNX 支持良好
        return F.relu(out)

# 2. 强大的 ResNet-CRNN 架构
class CRNN_ResNet_Final(nn.Module):
    def __init__(self, vocab_size):
        super(CRNN_ResNet_Final, self).__init__()
        self.cnn = nn.Sequential(
            nn.Conv2d(1, 64, kernel_size=3, stride=1, padding=1, bias=False),
            nn.BatchNorm2d(64), nn.ReLU(inplace=True),
            nn.MaxPool2d(kernel_size=2, stride=2), # 64x32x128

            ResNetBlock(64, 128, stride=(2, 1)),   # 128x16x128
            ResNetBlock(128, 256, stride=(2, 1)),  # 256x8x128
            ResNetBlock(256, 512, stride=(2, 2)),  # 512x4x64
        )
        # 512 * 4 = 2048
        self.rnn = nn.LSTM(2048, 256, bidirectional=True, num_layers=2, batch_first=True, dropout=0.5)
        self.fc = nn.Linear(512, vocab_size)

    def forward(self, x):
        x = self.cnn(x)
        x = x.permute(0, 3, 1, 2) # [B, W, C, H]
        x = x.flatten(2)          # [B, W, C*H]
        x, _ = self.rnn(x)
        x = self.fc(x)
        return x.log_softmax(2)

# 实例化新模型
model = CRNN_ResNet_Final(VOCAB_SIZE).to(DEVICE)
optimizer = optim.Adam(model.parameters(), lr=0.001)
criterion = nn.CTCLoss(blank=0, zero_infinity=True)
# 添加一个学习率调度器，这里使用余弦退火
scheduler = optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=EPOCHS, eta_min=0.00001)
print("✅ 已定义全新的、Web友好的CRNN模型架构。")
```

## 步骤 8: 训练模型

```python
# 定义解码与可视化函数
def decode_predictions(preds, int_to_char):
    texts = []
    preds = preds.argmax(2)
    for pred in preds:
        text = ""
        for i, token_index in enumerate(pred):
            token = int_to_char[token_index.item()]
            if token != "[BLANK]" and (i == 0 or token != int_to_char[pred[i-1].item()]):
                text += token
        texts.append(text)
    return texts
print("🚀🚀🚀 开始全新训练 (Web-Safe模型)... 🚀🚀🚀")
for epoch in range(EPOCHS):
    model.train()
    for images, labels, label_lengths in tqdm(train_loader, desc=f"Epoch {epoch+1}/{EPOCHS} [训练]"):
        images, labels, label_lengths = images.to(DEVICE), labels.to(DEVICE), label_lengths.to(DEVICE)

        optimizer.zero_grad()

        preds = model(images).permute(1, 0, 2)
        input_lengths = torch.full(size=(images.size(0),), fill_value=preds.size(0), dtype=torch.long)
        loss = criterion(preds, labels, input_lengths, label_lengths)
        loss.backward()
        optimizer.step()
        # 在每个epoch的训练循环结束后
        scheduler.step()
    model.eval()
    val_loss = 0
    with torch.no_grad():
        for i, (images, labels, label_lengths) in enumerate(val_loader):
            images, labels, label_lengths = images.to(DEVICE), labels.to(DEVICE), label_lengths.to(DEVICE)
            preds = model(images).permute(1, 0, 2)
            input_lengths = torch.full(size=(images.size(0),), fill_value=preds.size(0), dtype=torch.long)
            loss = criterion(preds, labels, input_lengths, label_lengths)
            val_loss += loss.item()

            if i == 0:
                pred_texts = decode_predictions(preds.permute(1, 0, 2), int_to_char)
                print(f"\n--- Epoch {epoch+1} 预测示例 ---")
                for j in range(min(5, len(pred_texts))):
                    real_text = "".join([int_to_char.get(c.item(), '') for c in labels[j][:label_lengths[j]]])
                    print(f"  真实: {real_text:<20} -> 预测: {pred_texts[j]}")
                print("-" * 30)
    val_loss /= len(val_loader);
    print(f"Epoch {epoch+1}/{EPOCHS} - 验证损失: {val_loss:.4f}")

    # 早停逻辑与模型保存
    if val_loss < best_val_loss:
        best_val_loss = val_loss
        torch.save(model.state_dict(), MODEL_SAVE_PATH)
        print(f"✅ 验证损失降低，最佳模型已保存至 {MODEL_SAVE_PATH}")
        patience_counter = 0
    else:
        patience_counter += 1
        if patience_counter >= PATIENCE:
            print(f"触发早停。")
            break
```

## 步骤 9: 导出ONNX模型

```python
# 1. 加载最佳模型
model.load_state_dict(torch.load("best_crnn_model.pth"))
model.eval()

# 2. 导出ONNX模型
dummy_input = torch.randn(1, 1, IMAGE_HEIGHT, IMAGE_WIDTH).to(DEVICE)
torch.onnx.export(model, dummy_input, "crnn_model.onnx", opset_version=11, input_names=['input'], output_names=['output'], dynamic_axes={'input': {0: 'batch_size'}, 'output': {0: 'batch_size'}})

print("✅ ONNX模型已导出为 crnn_model.onnx")
```

至此，您已经拥有了一个完整的、可复现的、带有验证和可视化的专业级模型训练流程。这个流程产出的ONNX模型，将是您项目成功的最后一块拼图。
